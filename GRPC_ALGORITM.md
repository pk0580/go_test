### Алгоритм работы отправки писем в приложении

В данном приложении реализовано два основных способа отправки уведомлений (email): **асинхронный** (через очередь Redis) и **синхронный** (напрямую через gRPC). Основная логика обработки и физической отправки писем вынесена в микросервис на языке Go.

---

### 🛠 Используемые технологии

*   **Laravel 12 (PHP 8.4)**: Основной бэкенд, инициирующий отправку.
*   **Go**: Высокопроизводительный сервис для обработки очередей и выполнения gRPC запросов.
*   **gRPC**: Протокол для синхронного взаимодействия между Laravel и Go.
*   **Redis**: Брокер сообщений для асинхронной очереди.
*   **MySQL**: Хранение истории сообщений и их статусов.
*   **Mailpit**: SMTP-сервер для тестирования (заглушка), перехватывающий все исходящие письма.
*   **Docker**: Вся инфраструктура развернута в контейнерах.

---

### 1. Асинхронная отправка (через очередь)

Это основной способ для массовых рассылок или уведомлений, не требующих мгновенного ответа.

#### Алгоритм:
1.  **Laravel**: Создает запись в таблице `messages` со статусом `pending`.
2.  **Laravel**: Формирует JSON с данными (`id`, `recipient`, `content`) и пушит его в Redis-список `messages_queue`.
3.  **Go-воркер**: Постоянно слушает очередь Redis (команда `BLPop`).
4.  **Go-воркер**: При получении сообщения десериализует JSON и передает задачу в пул внутренних воркеров (goroutines).
5.  **Go-воркер**: Выполняет отправку через `SMTPSender` (подключается к Mailpit).
6.  **Go-воркер**: Обновляет статус записи в MySQL (`sent` или `failed`) по `id`.

#### Ключевые файлы:
*   `app-laravel/app/Http/Controllers/MessageController.php`: Логика постановки в очередь.
*   `service-go/internal/worker/worker.go`: Диспетчер и обработчик задач в Go.
*   `service-go/internal/sender/sender.go`: Реализация SMTP клиента.

---

### 2. Синхронная отправка (через gRPC)

Используется, когда Laravel нужно немедленно узнать результат отправки или получить данные от Go-сервиса.

#### Алгоритм:
1.  **Laravel**: Вызывает метод `sendEmail` в классе `GrpcClientService`.
2.  **Laravel**: gRPC клиент формирует запрос `EmailRequest` (описан в `.proto` файле) и отправляет его по TCP на порт Go-сервиса.
3.  **Go-сервер**: Принимает запрос в методе `SendEmail`, вызывает `SMTPSender`.
4.  **Go-сервер**: Возвращает `EmailResponse` с результатом (`success: true/false`).
5.  **Laravel**: Получает ответ и может сразу вывести результат пользователю или лог.

#### Ключевые файлы:
*   `proto/sender.proto`: Определение контракта gRPC.
*   `app-laravel/app/Services/GrpcClientService.php`: Обертка над gRPC клиентом для Laravel.
*   `app-laravel/app/Grpc/SenderServiceClient.php`: Сгенерированный gRPC клиент.
*   `service-go/internal/grpcserver/server.go`: Реализация gRPC сервера на Go.

---

### 📋 Справочник классов и структур

#### Laravel (PHP)
- `GrpcClientService`: Высокоуровневый сервис. Используйте его в контроллерах или командах.
- `Message`: Eloquent модель для работы с таблицей сообщений.
- `TestGrpcCommand`: Консольная команда `php artisan test:grpc` для быстрой проверки связи с Go.

#### Go-сервис
- `Server` (в `grpcserver`): Обрабатывает входящие RPC вызовы.
- `Worker`: Логика чтения Redis и управления потоками обработки.
- `SMTPSender`: Инкапсулирует работу с протоколом SMTP.

---

### 🚀 Советы для новичка

1.  **Как проверить отправку?**
    Открой Mailpit в браузере (обычно `localhost:8025`). Все письма, отправленные через приложение, попадут туда.

2.  **Как добавить новое поле в письмо?**
    - Сначала измени файл `proto/sender.proto`.
    - Перегенерируй код для PHP и Go (команды обычно описаны в `README.md` или `Makefile`).
    - Обнови логику в `GrpcClientService.php` и `server.go`.

3.  **Логирование:**
    - Логи Laravel: `app-laravel/storage/logs/laravel.log`.
    - Логи Go: доступны через `docker compose logs service-go`.

4.  **Мониторинг:**
    В Go-сервисе встроены метрики (Prometheus), которые отслеживают количество обработанных сообщений и скорость отправки. Статус можно также проверить командой `php artisan test:grpc`.
